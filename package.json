{
  "name": "cut-thai-word",
  "version": "0.0.1",
  "description": "A custom application for cut thai word",
  "license": "UNLICENSED",
  "scripts": {
    "build": "now-sdk build",
    "deploy": "now-sdk deploy",
    "fetch": "now-sdk fetch",
    "dependencies": "now-sdk dependencies"
  },
  "devDependencies": {
    "@servicenow/eslint-plugin-sdk-app-plugin": "2.0.0",
    "@servicenow/glide": "26.0.1",
    "@servicenow/sdk": "^1.0.6",
    "eslint": "8.50.0"
  }
}
